# AI辅助BPMN流程建模系统 — 使用说明

> 适用版本：第一阶段（标准BPMN 2.0 MVP + 对话式修改）

---

## 1. 系统启动

### 1.1 启动后端（端口 8080）

```bash
cd ai-bpmn-platform/backend
mvn spring-boot:run
```

启动前确认 LLM 配置（`backend/src/main/resources/application.yml`）：

| 配置 | 说明 |
|---|---|
| `AI_BASE_URL` | 模型网关地址，当前默认 `https://api.deepseek.com` |
| `AI_API_KEY` | DeepSeek API Key（建议用环境变量注入，不要提交到代码库） |
| `AI_MODEL` | 模型 ID，当前默认 `deepseek-v4-flash` |
| `AI_MOCK_ENABLED` | `false`=调用真实LLM；`true`=无Key演示模式 |

推荐启动方式（Key 走环境变量）：

```bash
export AI_API_KEY=sk-你的密钥
export AI_MOCK_ENABLED=false
mvn spring-boot:run
```

API 文档：启动后访问 <http://localhost:8080/swagger-ui.html>

### 1.2 启动前端（端口 5173）

```bash
cd ai-bpmn-platform/frontend
npm install     # 首次
npm run dev
```

浏览器访问 <http://localhost:5173>

---

## 2. 界面布局

```text
┌──────────────────────────────────────────────────────────┐
│ 流程名称  撤销 恢复  导入  版本历史  导出BPMN  导出工作区  │
├──────────────┬───────────────────────────────┬───────────┤
│ BPMN元素面板 │        BPMN编辑画布           │ AI流程助手 │
│ (左侧工具条) │  (拖拽/连线/编辑节点)          │ 对话记录  │
│              │                               │ 澄清问答  │
│              │                               │ 修改预览  │
├──────────────┴───────────────────────────────┴───────────┤
│ 校验结果：错误/警告/元素定位                              │
└──────────────────────────────────────────────────────────┘
```

---

## 3. AI 生成流程（推荐起步方式）

### 3.1 基本用法

1. 在右侧 **AI 流程助手** 输入框中用自然语言描述流程；
2. 点击 **发送**；
3. AI 分析描述后返回两种结果之一：
   - **澄清问题**：信息不足时（如未说明拒绝路径、角色、结束条件），面板出现问题列表，逐题作答后点击 **提交答案并生成**；
   - **直接生成**：流程图自动渲染到画布，并创建版本快照（R1、R2…）。

### 3.2 描述技巧（生成质量更高）

好的描述应包含：**角色、步骤、判断分支、拒绝/异常路径、结束条件**。

```text
❌ 模糊：  帮我生成一个报销流程
✅ 具体：  员工填写报销单，直属主管审批，金额超过5000元需要总监加签，
          审批通过后财务打款并邮件通知员工，拒绝则退回员工修改
```

说明要点：
- **角色** → 生成泳道（Lane）
- **判断条件** → 生成排他网关（如"金额是否超过5000元"）
- **并行步骤** → 生成并行网关（如"安全评审和架构评审同时进行"）
- **拒绝/退回** → 生成回退分支

### 3.3 修改已有流程

画布上有流程后，继续用对话修改：

```text
· 在主管审批之后增加一个合规审查环节
· 审批不通过时退回申请人修改
· 将"部门主管"改为"科技主管"
· 删除"人工登记"环节
· 安全评审和架构评审并行执行
```

AI 不会直接改动画布，而是生成**修改预览**：
- 绿框 = 新增元素，橙框 = 修改元素，红虚线 = 待删除元素
- 预览列出新增/修改/删除的节点与连线，以及修改后模型的校验结果
- 点击 **应用修改** 生效（并创建快照），点击 **放弃修改** 画布保持不变

### 3.4 解释 / 优化 / 检查

在输入框使用关键词开头即可（画布需有流程）：

| 输入开头 | 功能 |
|---|---|
| `解释…` / `说明…` | AI 解释当前流程的逻辑 |
| `优化…` / `建议…` | AI 提出流程简化优化建议 |
| `检查…` / `校验…` | AI 进行业务语义检查 |

---

## 4. 画布手工编辑

| 操作 | 方式 |
|---|---|
| 添加节点 | 左侧面板拖拽元素到画布（开始/结束事件、任务、网关、池等） |
| 移动节点 | 按住节点拖动 |
| 连线 | 悬停节点出现箭头锚点，拖到目标节点 |
| 修改名称 | 双击节点直接编辑 |
| 删除 | 点选节点后按 `Delete` 键 |
| 缩放画布 | 工具栏"放大/缩小/适应画布"，或 Ctrl+滚轮 |
| 全图自动布局 | 工具栏 **全图自动布局**（调用后端布局服务，重新排列全图） |

**注意事项**：
- 带 Pool（池）的流程中，节点**不能拖出池边界**（BPMN 规范约束）；
- 手工编辑会自动同步到内部语义模型（ProcessModel），编辑后可直接继续用 AI 修改；
- 手工编辑后底部校验面板会实时刷新错误/警告。

---

## 5. 校验与定位

点击工具栏 **校验** 或编辑后自动触发，结果在画布下方展示：

| 示例 | 含义 |
|---|---|
| `BPMN-002 流程至少包含一个结束事件` | 结构错误（红色），需修复 |
| `BPMN-005 排他网关建议配置默认路径` | 建议警告（橙色） |
| `BPMN-004 排他网关的非默认分支应有明确条件` | 分支缺条件 |

点击错误项后的 **定位** 链接可聚焦相关元素。

主要规则：开始/结束事件必须存在、任务必须有进出连线、排他分支必须有条件、不允许孤立节点、不允许无法到达结束的路径、ID 必须唯一等（详见 DESIGN.md 第16节）。

---

## 6. 版本快照与撤销恢复

- 每次成功操作都会创建本地快照：**AI创建 / AI修改确认 / 导入 / 版本恢复**（手工拖拽微调不建快照）；
- 工具栏 **撤销 / 恢复**：在快照之间回退与前进；
- 工具栏 **版本历史**：打开快照时间线，点击任意历史版本的"恢复"回到该版本（恢复本身也会生成新快照）；
- 默认保留最近 **20 个**快照，仅存于当前浏览器（刷新页面会清空，重要流程请及时导出）。

---

## 7. 导入与导出

| 操作 | 说明 |
|---|---|
| **导出BPMN** | 保存标准 `.bpmn` 文件（含图形布局，可导入 Camunda/Flowable Modeler 等工具） |
| **导出工作区** | 保存 `.json` 文件（含流程、对话记录、全部版本快照） |
| **导入** | 支持 `.bpmn` / `.xml` / `.json` 三种文件 |

工作区 JSON 导出后再导入，可完整恢复对话与版本历史，适合换机器或备份。

---

## 8. AI 修改的保护机制

| 机制 | 说明 |
|---|---|
| 先预览后应用 | AI 修改必须经差异预览确认，未确认前画布不变 |
| 布局保留 | AI 修改不移动你手工调整过的节点；新增节点只做局部布局 |
| 过期保护 | 若预览生成后你又改了画布（revision 变化），应用时提示"AI响应已过期"并丢弃 |
| 白名单操作 | AI 只能通过 15 种受控操作修改流程，不能直接改 XML |
| 澄清优先 | 指令不明确时 AI 会先追问，不擅自猜测 |

---

## 9. 常见问题（FAQ）

**Q1：点"发送"后很久没反应？**
DeepSeek 生成一般 10~40 秒，复杂流程可能更久。若持续无响应，检查后端日志是否有模型调用错误（如 401 表示 API Key 无效）。

**Q2：生成时报"请求失败：模型输出…无效"？**
模型偶发输出格式异常，系统会自动重试一次。重试仍失败时，换一种更清晰的描述重试即可。

**Q3：拖拽节点提示红色 "flow elements must be children of pools/participants"？**
节点被拖出了池（Pool）边界，拖回池内即可。

**Q4：刷新后流程不见了？**
工作区仅存于浏览器内存（设计如此，服务端不保存）。刷新前请先"导出工作区"或"导出BPMN"。

**Q5：想重新生成一个全新流程，但画布上已有内容？**
- 画布为空/只有孤立节点时，直接说"生成一个××流程"即可整体生成；
- 画布上已有完整流程时，生成指令会走"修改"路径（保护已有工作）；如需全新流程，先导出备份，再删除画布上全部元素后重新生成。

**Q6：连接其他 OpenAI 兼容模型？**
修改 `application.yml` 中 `base-url` 与 `model` 即可，例如通义千问、Moonshot 等，只要兼容 OpenAI Chat Completions 协议。

---

## 10. 快速上手清单

1. 启动后端（配置 DeepSeek Key）与前端；
2. 打开 <http://localhost:5173>；
3. 在 AI 助手输入一段包含角色、审批、拒绝路径的流程描述；
4. 回答澄清问题，等待流程图生成；
5. 画布上拖拽微调布局；
6. 用对话继续修改（体验差异预览 → 应用）；
7. 点击"校验"确认无错误；
8. 导出 `.bpmn` 文件存档。
